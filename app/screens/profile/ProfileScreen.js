import { Platform, Text, View } from "react-native";

import { SafeAreaView } from "react-native-safe-area-context";

function ProfileScreen() {
  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: "#ff6d03" }}>
      <Text>aaaaaaaaa</Text>
    </SafeAreaView>
  );
}

export default ProfileScreen;
